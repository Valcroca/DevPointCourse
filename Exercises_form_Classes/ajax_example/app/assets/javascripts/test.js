PROBLEM: Append users first name to an unordered list using jquery and ajax (can be done in raw js or coffeescript)

endpoint: GET /users
example response: [{first_name: 'Jake', last_name: 'Sorce'}, {first_name: 'Test', last_name: 'User'}]
example HTML: <ul id="users_list"></ul>

$.ajax({
  type: 'GET';
  dataType: 
  data:
  success:
  error:
})